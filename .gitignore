.DS_Store
.env
.env.*
!.env.example
node_modules/
.next/
out/
coverage/
dist/
build/
*.tsbuildinfo
next-env.d.ts
*.log
.gstack/

# design round render output (variants + rankings ARE the git record)
design/rounds/*/renders/

# shader-probe screenshots (`--keep`). The MEASUREMENTS belong in D6's and D7's
# tables, the PNGs are scratch.
.probe/
.probe-halftone/

# Conductor's cross-agent scratch space.
.context/
